spring:
  datasource:
    url: jdbc:postgresql://postgressvc:5432/appdb?currentSchema=public
    username: appuser
    password: strongpasswordapp
    jpa:
      show-sql: true
      properties:
        hibernate:
          format_sql: true
